{
    "name": "kisa-ros-humble",
    "image": "kisa-ros:humble", 
    //"forwardPorts": [3000],
    //"postCreateCommand": "",
    "customizations": {
      "vscode": {
        "extensions": ["ms-python.python", "ms-python.debugpy", "ms-toolsai.jupyter"]
      }
    },
    "runArgs": [
      "--net", "host", 
      "--privileged",
        "-e", "DISPLAY=${env:DISPLAY}",
        "-v", "/tmp/.X11-unix:/tmp/.X11-unix",
        "--gpus",
        "all"
      ]
      //,
      //"remoteUser": "${localEnv:USER}"
}
  